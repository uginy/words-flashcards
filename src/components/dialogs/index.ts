// Dialog components
export { DialogCard } from '../DialogCard';
export { DialogInterface } from '../DialogInterface';
export { DialogGenerator } from '../DialogGenerator';
export { DialogsList } from '../DialogsList';
export { DialogsTab } from '../DialogsTab';

// Re-export types for convenience
export type {
  Dialog,
  DialogCard as DialogCardType,
  DialogParticipant,
  DialogGenerationSettings,
  DialogLevel,
  ParticipantGender
} from '../../types';